# ─── ClaimGuard 360° Backend — Runtime Dependencies ────────────────────────────
# Web framework & ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
aiosmtplib==3.0.2

# Data / ORM / migrations
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Validation / settings (Pydantic v2)
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Security
PyJWT==2.10.1
passlib[argon2]==1.7.4
argon2-cffi==23.1.0

# Caching / broker / background work
redis==5.2.1
celery==5.4.0

# Machine learning — FraudShield trained ensemble (XGBoost + IsolationForest).
# TreeSHAP explanations use XGBoost's native pred_contribs (no `shap` package).
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2

# Observability
structlog==24.4.0
prometheus-client==0.21.1

# Utilities
python-dotenv==1.0.1
httpx==0.28.1
python-multipart==0.0.20
